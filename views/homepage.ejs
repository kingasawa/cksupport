

<div class="container-fluid">
  <div class="row content">
    <!--left side-->
    <div id="leftSidenav" class="sidenav">
      <a href="javascript:void(0)" class="closebtn close-btn">&times;</a>
      <a href="javascript:void(0)" class="sr-only closebtn open-btn">&times;</a>

      <a href="#">About</a>
      <a href="#">Services</a>
      <a href="#">Clients</a>
      <a href="#">Contact</a>
    </div>
    <!--right side-->
    <div id="mainSidenav">
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <div class="navbar-header">
            <a class="navbar-brand" href="#">WebSiteName</a>
          </div>
          <ul class="nav navbar-nav">
            <li class="active"><a href="#">Home</a></li>
            <li><a href="#">Page 1</a></li>
            <li><a href="#">Page 2</a></li>
            <li><a href="#">Page 3</a></li>
          </ul>
        </div>
      </nav>
      <table class="table" id="guest-info-table">
        <tbody>
        <% if (allGuest) { _.each(allGuest,function(guest) { %>
          <tr class="guest<%= guest.name %>">
            <td class="guest-name">Guest<%= guest.name %></td>
            <td class="guest-ip"><%= guest.ip %></td>
            <td class="guest-title"><%= guest.title %></td>
            <td class="guest-url"><%= guest.url %></td>
            <td class="guest-browser">browser</td>
            <td class="guest-status"><%= guest.status %></td>
          </tr>
        <% })} %>


        </tbody>
      </table>
    </div>

</div>
</div>


